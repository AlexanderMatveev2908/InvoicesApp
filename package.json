{
  "name": "invoices-app",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^2.9.16"
  },
  "scripts": {
    "install_pkg": "turbo run install_pkg --parallel",
    "dev": "turbo run dev --parallel",
    "build": "turbo run build --parallel"
  }
}
