{
  "name": "server",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "install_pkg": "dotnet restore",
    "dev": "dotnet watch run --launch-profile https",
    "build": "dotnet build && dotnet publish -c Release",
    "start": "ASPNETCORE_URLS=https://localhost:3000 dotnet bin/Release/net9.0/publish/InvoicesApp.dll"
  }
}
